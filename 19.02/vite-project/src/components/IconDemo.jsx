import { Delete, HomeIcon } from '@mui/icons-material';
import { IconButton } from '@mui/material';

export default function IconDemo() {
    return (
        <IconButton>
            <Delete />
            <HomeIcon />
        </IconButton>
    );
}